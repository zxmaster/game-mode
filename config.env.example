# Docker Compose Project Configuration
# List the Docker Compose project names to manage (one per line)
# These projects will be started by work-mode.ps1 and stopped by game-mode.ps1

# Copy this file to config.env and customize with your project names
# Example projects:
librechat
qdrant

# Add your own Docker Compose projects below:
# my-project-name
# another-project
